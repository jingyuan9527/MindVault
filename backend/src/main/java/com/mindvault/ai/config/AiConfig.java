package com.mindvault.ai.config;

import org.springframework.context.annotation.Configuration;

/**
 * AI 模块配置（预留）
 *
 * 当前为空配置类，仅用于触发组件扫描。
 * 后续可在此添加 AI 相关全局配置 Bean，如默认模型参数、重试策略等。
 */
@Configuration
public class AiConfig {
}